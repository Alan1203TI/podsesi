Cole em Firebase > Firestore Database > Regras e clique em Publicar:

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /episodios/{episodio=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
