REGRAS DO STORAGE

Cole em: Firebase Console > Storage > Regras

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
